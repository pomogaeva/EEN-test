import { Typography } from '@mui/material';
import React from 'react';
import Layout from '../../atoms/Layout/Layout';

const Home = () => {
	return (
		<Layout>
      <Typography variant='h2'>Welcome to the CameraManager!</Typography>
    </Layout>
	);
};

export default Home;